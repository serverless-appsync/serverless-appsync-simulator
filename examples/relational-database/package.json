{
  "name": "external-functions",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "offline": "sls offline start",
    "db:start": "docker compose up -d",
    "db:seed": "docker compose exec db psql -U postgres -d local -a -f './seed.sql'"
  },
  "dependencies": {
    "serverless-appsync-plugin": "^1.3.1",
    "serverless-offline": "^6.7.0"
  },
  "devDependencies": {
    "serverless": "^2.53.1"
  }
}
